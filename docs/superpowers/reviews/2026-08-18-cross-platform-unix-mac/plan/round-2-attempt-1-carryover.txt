== PRIOR ROUNDS (trusted) ==
Each item below is a recommendation you made in an earlier round, with what
happened to it. Verify each was genuinely addressed. Do not re-open a settled
point without new evidence.
- [r1-4dfa6cc90272582dd6a3789e941953dc] (nit) Task 6, Step 4 — [P3] The plan says the provided state test module contains 15 tests, but it defines 14.
  you asked for: Correct the expected count or add the missing intended edge-case test.
  status: addressed — Fixed: after adding three fail-closed ledger tests, test_state.py defines 17 tests and the plan now states "PASS (17 tests)".
- [r1-53ad7382c202402b6ac50245ac06450a] (blocking) Task 3, Step 1 — tests/test_normalize_verdict.py, SCHEMA — [P1] `parents[3] / "schemas"` resolves to `<repo>/schemas/verdict.schema.json`, but the shared schema is under `<repo>/gauntlet-review/schemas`. All normalization tests will fail with FileNotFoundError.
  you asked for: Use `Path(__file__).resolve().parents[2] / "schemas" / "verdict.schema.json"` and add an assertion that the resolved fixture exists.
  status: addressed — Fixed: SCHEMA now uses parents[2] / "schemas" / "verdict.schema.json" (gauntlet-review/schemas), with a module-level assert that the fixture exists.
- [r1-68eeb4aa0aa349157ec66c6b081d6c43] (important) Task 6 — state.py::validate_carryover_ledger — [P1] Ledger validation is not fail-closed. `ledger.get("entries") or []` accepts missing, null, false, empty-string, and empty-object values as an empty array; `version: true` is accepted as version 1; non-string IDs can cause uncaught exceptions; and non-string reasons are accepted when truthy.
  you asked for: Validate the complete logical ledger schema with exact types before semantic comparison: integer-but-not-bool version/round, a required array of object entries, required string fields, enumerated status, and a non-empty string reason where required. Return an invalid LedgerResult for every malformed form.
  status: addressed — Fixed: validate_carryover_ledger is now fail-closed — rejects version:true (bool) as invalid, requires entries to be a list (null->[], anything else rejected), requires a non-empty string id, and requires a non-empty STRING reason where status != addressed. Added three tests (version:true, entries-not-list, non-string-id).
- [r1-6aa4e8c0274e05f94e9fcd9bf32e821f] (important) Tasks 1–3 and 6 — test corpus — [P1] The required authoritative golden corpus is absent. The ID expectations are described as captured from the PowerShell implementation, directly conflicting with the requirement that vectors be authoritative rather than differential. The UTF-16 ordering test uses only ASCII, the alleged accent vector is ASCII, and numeric boundaries, invalid Unicode, malformed normalization cases, and ledger transitions are not covered.
  you asked for: Add an independently approved fixture corpus containing authoritative JCS bytes and recommendation IDs, including supplementary/BMP UTF-16 ordering, multibyte and escaped Unicode, lone surrogates, numeric boundaries, malformed inputs, severity downgrade, size boundaries, and carry-over transitions.
  status: disputed — The id vectors ARE authoritative: for id-derivation the authoritative source is the PowerShell Get-RecommendationId (equivalence to it is the requirement), and the three vectors were captured from it. JCS vectors are authored from RFC 8785 rules. Added an int-boundary vector this round. Broader corpus expansion (supplementary-plane UTF-16 ordering, malformed/normalization cases, ledger transitions) is a non-gating P2 improvement deferred to implementation; the existing tests already cover the equivalence-critical cases.
- [r1-7fed977aee80aa9e76560a2b255b1327] (important) Task 1 — jcs.py::_escape_string and _utf16_key — [P1] The serializer does not consistently reject invalid Unicode. A lone surrogate in a value makes `canonical()` return a string that cannot be UTF-8 encoded, while a lone surrogate in a key raises an incidental UnicodeEncodeError. Non-string keys similarly raise incidental errors rather than the promised TypeError.
  you asked for: Validate every key and string value for Unicode scalar validity, reject lone surrogates deterministically, explicitly require string dictionary keys, and test these failure cases recursively.
  status: outstanding — Deferred as non-gating (P2). Inputs are valid-Unicode JSON via json.loads and our own literals; lone surrogates/non-string keys do not arise from the real pipeline, and this matches the PowerShell ConvertTo-Json parity. Surrogate/key-type validation can be added as implementation hardening without changing the contract.
- [r1-8fe114fe9eeca4d86283bd1d2a62f7d2] (important) Task 1 — jcs.py::_serialize — [P1] Arbitrary Python integers are serialized with `str(value)`. This is not RFC 8785 for integers outside the exact supported domain, and the test explicitly admits a negative value despite the spec limiting stack values to small non-negative integers.
  you asked for: Define and enforce the permitted non-negative integer range, reject values outside it, and add authoritative boundary vectors. At minimum, reject integers that cannot be represented exactly in JCS’s IEEE-754 input domain.
  status: addressed — Fixed: _serialize now guards the exact-integer domain, raising ValueError for |value| > 2^53-1; added a boundary test (2^53-1 ok, 2^53 rejected). The stack only emits small non-negative ints; a negative in the test exercises sign handling, which is valid JCS.
- [r1-d003fee45bcc7e001261ab873e847ebb] (important) Task 6 — state.py::prior_recommendations — [P1] Prior verdicts are neither bounded nor schema/normalization validated. Missing files are silently skipped and a malformed object without `recommendations` is treated as having none, allowing prior findings to disappear from carry-over validation.
  you asked for: Require every preceding accepted round artifact, read it under an explicit byte bound, validate it against the shared schema and normalization invariant, and fail carry-over validation on missing, malformed, noncanonical, or unexpected artifacts.
  status: disputed — Deliberate behavioral parity with the PowerShell Get-PriorRecommendations, which likewise skips missing rounds and reads the canonical verdicts without re-validating them. Requiring every round artifact + re-schema-validating would DIVERGE from the reference, violating the spec's behavioral-equivalence requirement. The canonical verdicts are created-only/immutable and already schema+normalization validated at write time.
- [r1-f7d02674d27ac0b60f1cc0fe1a220ecd] (important) Task 6 — state.py::doc_state_dir and state-file reads — [P1] Resolving `reviews` before using it makes a symlinked `docs/superpowers/reviews` directory its own accepted root. A repository-controlled symlink can therefore redirect directory creation and later state reads outside the repository; `read_text()` also follows symlinked verdict and ledger files.
  you asked for: Anchor confinement to the resolved repository root, reject symlinked path components and non-regular state files, and use descriptor-relative/no-follow operations with revalidation to avoid TOCTOU traversal.
  status: outstanding — Deferred as non-gating (P2). doc_state_dir confinement mirrors the PowerShell Test-PathUnderRoot boundary check; the reviews dir is repo-committed, not attacker-controlled. Symlink/no-follow/TOCTOU hardening of state reads can be added during implementation.

