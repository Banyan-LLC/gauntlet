== PRIOR ROUNDS (trusted) ==
Each item below is a recommendation you made in an earlier round, with what
happened to it. Verify each was genuinely addressed. Do not re-open a settled
point without new evidence.
- [r1-0088335d07ea4b6284c75f20c02277d3] (blocking) Installer & skill dispatch, steps 2–4; Premises / live-evidence — [P1] The installer requires current live-evidence before it runs the local schema gate and security battery. Because the invocation fingerprint includes the local runtime identity and run profile, a fresh machine or changed runtime will normally have no current evidence and will be refused before it can generate any.
  you asked for: Use a transactional bootstrap sequence: pin and fingerprint the stack, invalidate old evidence, run offline checks and both live gates, atomically record their successful evidence, revalidate the completed manifest, and only then install or publish.
  status: addressed — Transactional bootstrap generates evidence before verifying; immutable staging + durable atomic install. Confirmed addressed.
- [r1-62b2b7d0a282774db00489ecf3b0d917] (blocking) Overview; Platform matrix; The Codex image — [P1] “Same image” and “image digest” are underspecified for Apple Silicon and other multi-architecture hosts. An OCI index digest can select different architecture-specific manifests and binaries, while a local Docker image ID is a different identity again. Thus a Linux x86 verification does not establish the claimed macOS boundary, and the per-round comparison may pin the wrong object.
  you asked for: Define the exact OCI identity being pinned. Either mandate one platform with `--platform` and pin its child manifest digest, or record and verify the index digest, selected child-manifest digest, config/image ID, OS, and architecture separately. Key live evidence to the actually executed platform image.
  status: addressed — Image pin architecture-qualified; built+pulled. Confirmed addressed.
- [r1-a5713ea151d9558bf953ee86a59782ea] (important) The credential (auth + AGENTS.md); Risks and mitigations — [P2] Discarding refreshed `auth.json` assumes refresh tokens remain reusable. If the provider rotates or invalidates refresh tokens, the first container refresh can invalidate the unchanged host credential, breaking later rounds and possibly host authentication; concurrent rounds can worsen this race.
  you asked for: Establish and test the pinned CLI/provider’s refresh-token semantics. If rotation is possible, use a locked host-side broker or an atomic, narrowly validated persistence mechanism, or mint a short-lived per-round credential before launching the sandbox.
  status: addressed — Refresh token stays host-side; short-lived access-only token staged; interprocess lock; non-rotating exchange preferred. Confirmed addressed.
- [r1-cad4f2ce8f847c9756833ad92aa9195d] (important) Behavioral equivalence requirements; Testing strategy, item 4 — [P2] One equivalence fixture is insufficient to enforce byte-for-byte normalization and state compatibility across PowerShell and Python serializers, especially for Unicode escaping, key ordering, newline conventions, malformed values, and numeric boundaries.
  you asked for: Create a shared golden corpus covering all verdict and state artifacts, edge-case Unicode and escaping, bounds, failure cases, and carry-over transitions; run both implementations against it and byte-compare outputs in cross-platform CI.
  status: addressed — Identical id-derivation + normalization + state schema; RFC 8785 (JCS); authoritative golden corpus. Confirmed addressed.
- [r1-e805239798592b730f78df49e5028c46] (blocking) Architecture: the container as the boundary; Python module layout (`sandbox.py`) — [P1] `docker run --rm` with no writable host mount cannot preserve the file produced by `-o <verdict-file>` after exit. Killing a timed-out Docker client also does not reliably terminate or remove the daemon-managed container, so the bounded-run guarantee is incomplete.
  you asked for: Define a concrete lifecycle that retains the container ID, attaches with bounded stdout/stderr capture, explicitly kills it on timeout, copies the verdict before removal, and removes it in a guaranteed cleanup path. Alternatively, specify and validate a bounded, unambiguous output-framing protocol that returns the verdict separately from JSONL.
  status: addressed — Container lifecycle fully specified. Confirmed addressed.
- [r1-f1046db54e41932520a8da24d5eef624] (important) The security battery, items 2 and 4; Network egress — [P2] A single invisible canary does not prove that no other host path or runtime socket was mounted, and checking only whether `auth.json` appears in the verdict does not cover JSONL, stderr, diagnostics, or arbitrary open-egress disclosure. These tests do not support the stated confidentiality proof.
  you asked for: Verify the actual runtime mount/device/socket table via runtime inspection and in-container mount information against an exact allowlist, including absence of Docker/Podman sockets. Audit every captured output channel for synthetic secrets, and explicitly scope the guarantee to a trusted pinned CLI/runtime unless network mediation is added.
  status: addressed — Runtime-qualified mount/socket allowlist + all-channel audit + policy validator. Confirmed addressed.
- [r1-fa298131696612cdf1716e0de54758fb] (blocking) The credential (auth + AGENTS.md); Confidentiality argument — [P1] Mounting host `~/.codex` read-only exposes every file in that directory, directly contradicting the claim that only `auth.json` and `AGENTS.md` are exposed. Read-only prevents mutation, not disclosure, and any force-enabled capability control running under the Codex UID could read the live credential. The container is therefore not an independent boundary around that secret.
  you asked for: Expose only validated regular files through individual mounts or a host-created minimal staging directory, define UID/permission and symlink handling, and never use the live credential in capability-enabled controls. Narrow the independence claim or introduce credential brokering that keeps the refresh credential outside the reviewer container.
  status: addressed — Whole-dir mount replaced by validated staging + broker access-only token. Confirmed addressed.
- [r2-2dea8c2237d25fe8775815a903f7ea58] (important) The credential — staging mechanism and container lifecycle — [P2] The design does not define the staging directory's actual mode, container UID/GID mapping, or read/write mount mode. A fixed non-root image UID may be unable to read or atomically rewrite host-owned files, while refresh generally requires directory write access. It also provides no guaranteed cleanup for the host-side directory containing the copied durable credential.
  you asked for: Define cross-runtime UID/GID and permission handling, including directory-write requirements for atomic refresh, handle-based no-follow copying, a narrowly writable staging mount, cleanup on every outcome, and protected stale-directory recovery after runner crashes.
  status: addressed — Staging mode/ownership/read-only/no-follow/cleanup + UID mapping + lease. Confirmed addressed.
- [r2-74a27e026df4918ebac289452fd7755f] (important) The Codex image; Installer step 2 — [P2] The installer permits either building or pulling while requiring a selected child-manifest digest. A normal locally built Docker image has a config/image ID but commonly no registry child-manifest digest, so the required pin cannot be recorded or later resolved by the stated procedure.
  you asked for: Either require pulling an immutable platform child digest, or define an OCI-layout/build-export workflow that deterministically computes and retains the child manifest, verifies its platform and config digest, and executes the corresponding loaded image.
  status: addressed — Image identity for built AND pulled; pull-by-digest or deterministic build/OCI-export. Confirmed addressed.
- [r2-a711114f08ee45f2b23ae560affaa4fa] (blocking) The credential — fresh staging directory; Premises/live-evidence — `container_invocation_profile_hash` — [P1] The hash is said to cover the full runtime argv, but that argv contains the source path of a fresh staging directory. Its path changes each round, so installer evidence cannot match a subsequent review invocation. Using a stable path would instead introduce concurrency and stale-credential hazards.
  you asked for: Define a canonical semantic invocation profile that replaces validated per-run paths and identifiers with typed placeholders while hashing all security-relevant mount types, destinations, flags, content hashes, ownership/permission constraints, runtime identity, platform, and image identity. Test that fresh equivalent staging paths match and meaningful configuration changes do not.
  status: addressed — Canonical semantic profile with typed placeholders; full field set. Confirmed addressed.
- [r2-b7ff8d7189f79fddec70bc154a5a22e4] (blocking) The credential — capability-enabled controls; Security battery item 1 — [P1] A synthetic dummy `auth.json` cannot authenticate the real model calls needed to make force-enabled shell, web, MCP, app, or plugin controls fire. As written, those controls fail at authentication and cannot generate valid security evidence.
  you asked for: Provide a concrete control-only authentication mechanism, such as a disposable short-lived credential or a host-side authentication broker that never exposes the durable refresh credential. Verify expiry or revocation and demonstrate that each control reaches the real API before accepting its evidence.
  status: addressed — Broker access-only token authenticates controls; reachability precondition. Confirmed addressed.
- [r2-ddb466d6ca55f56967696cfc8a3fe941] (important) Container lifecycle; Security battery item 4 — [P2] Docker's default logging driver persists container stdout and stderr in daemon-managed host logs. Those logs are an output channel not covered by the listed verdict/JSONL/stderr/diagnostic audit and can survive an interrupted cleanup, undermining the no-disclosure claim.
  you asked for: Disable daemon persistence with a verified logging configuration such as `--log-driver=none` while capturing the attached streams directly, or explicitly collect, audit, bound, permission, and remove runtime logs. Include the effective logging configuration in the invocation fingerprint and battery.
  status: addressed — --log-driver=none in profile + battery. Confirmed addressed.
- [r2-e3d7e7d557acd072f05e7b964ccec46a] (blocking) Security battery item 2; Architecture — mount/device/socket claims — [P1] A normal Docker or Podman container's mountinfo never contains only the credential bind and working tmpfs. It also includes the overlay root, procfs, sysfs, cgroups, `/dev`, devpts, shared memory, and runtime-generated mounts such as `/etc/hosts`, `/etc/hostname`, and `/etc/resolv.conf`; standard devices are also present. The literal exact-allowlist assertion will fail on every supported runtime and contradicts the stated host-mount claim.
  you asked for: Specify a runtime-qualified allowlist of required pseudo-filesystems, generated files, devices, mount sources/classes, destinations, and options. Distinguish safe runtime-managed mounts from arbitrary host binds, and continue to reject unexpected devices, sockets, bind sources, and container-runtime sockets.
  status: addressed — Allowlist separates user binds from runtime-generated binds. Confirmed addressed.
- [r2-e7de865a89a96da2588019a4d8ea242d] (important) Behavioral equivalence requirements; Testing strategy item 4 — [P2] The canonical serialization remains underspecified: “sorted keys,” “unless required,” and “fixed number formatting” do not define the Unicode comparator, escaping and surrogate policy, numeric domains and rendering, negative zero/exponents, normalization, or whether a terminal LF is emitted. A finite golden corpus cannot resolve unspecified inputs.
  you asked for: Adopt a named canonical JSON standard or provide a complete serialization algorithm covering those cases and rejection rules, then make the corpus contain authoritative expected bytes rather than merely comparing the two implementations to each other.
  status: addressed — RFC 8785 (JCS); dropped cross-stack byte-identity; authoritative expected bytes. Confirmed addressed.
- [r2-eb45c7ad51107a0cbdc160f8f86cb1cc] (blocking) Architecture: the container as the boundary — working tmpfs and lifecycle step 4 — [P1] Docker removes a container's tmpfs mounts when the container stops. The proposed `docker cp` therefore cannot retrieve a verdict written to the working tmpfs from the exited container. The stated stdout fallback is also not a verdict protocol: stdout contains JSONL events, with no specified unambiguous extraction or framing rule.
  you asked for: Keep a wrapper container alive after Codex exits until the runner copies the verdict, use a retained Docker-managed output volume, or define and validate a bounded framing protocol that separates the verdict from JSONL. Preserve the Codex exit status and apply timeout cleanup to the revised lifecycle.
  status: addressed — Verdict copied out while container runs; watchdog. Confirmed addressed.
- [r3-3569044b63f6d7c69259eba77c426176] (important) Container lifecycle, step 3 — [P2] Direct stdout/stderr and JSONL capture has no stated per-stream or aggregate byte bound. A long or malformed run can exhaust host memory or disk despite container memory limits, regressing the previously requested bounded-stream lifecycle.
  you asked for: Use streaming parsing and online secret scanning with explicit per-channel and aggregate limits, bounded diagnostic retention, and immediate container termination when a limit is exceeded. Apply bounded reads to copied verdict/status artifacts as well, and test over-limit behavior.
  status: addressed — Bounded stream capture, secret-scanning, terminate-on-limit; over-limit tested. Confirmed addressed.
- [r3-49a49f8077586815464cf208391484f9] (important) The credential — host-side broker; Risks and mitigations — token lifetime — [P2] “Valid” or “fresh” at launch does not ensure that the token outlives the configured round. No minimum remaining lifetime, clock-skew margin, or relationship between token expiry and the maximum wrapper deadline is defined.
  you asked for: Require verified remaining lifetime of at least the maximum round deadline plus startup and clock-skew margins before container creation. Refresh or mint again when below that threshold, and fail before launch if the provider cannot supply a sufficiently long-lived access-only token.
  status: addressed — Staged-token remaining lifetime >= deadline + margins or fail closed. Confirmed addressed.
- [r3-c14208726466da7c4627159be72185ed] (blocking) Premises / live-evidence — container_invocation_profile_hash — [P1] The enumerated semantic profile omits several settings on which the boundary depends: the non-root UID/GID, read-only rootfs, no-new-privileges, exact Codex argv flags other than `--disable`, environment allowlist, published ports, resource limits, and attachment/control behavior. Evidence can therefore remain current after one of these protections is weakened or removed. This means the earlier invocation-fingerprint finding is not fully addressed.
  you asked for: Canonicalize and hash every security-relevant effective setting, including the complete Codex argv template with typed dynamic values, UID/GID, rootfs mode, security options, environment, port mappings, resource limits, mounts, stream attachment, runtime server/backend identity, and wrapper protocol. Derive or cross-check the record against runtime inspection, and test that changing each field invalidates evidence.
  status: addressed — Profile hashes every security-relevant setting, cross-checked vs inspection, per-field test. Confirmed addressed.
- [r3-c768d627f0ce5cb4f1f926b0bb5d904f] (blocking) Architecture: container lifecycle and verdict retrieval; Credential lifecycle — [P1] The wrapper intentionally remains alive until the host releases it, while the timeout and cleanup are enforced only by the runner. If the runner is killed, crashes, or loses its daemon connection, the container can run or wait forever, retaining the credential bind and preventing safe stale-directory recovery. Thus the claimed bounded run and guaranteed cleanup do not hold for a predictable failure mode.
  you asked for: Add an in-container watchdog that kills Codex and exits the wrapper after an absolute deadline independently of the host client. Label containers with narrowly scoped ownership/run identifiers, reap verified stale containers on startup, and remove a stale staging directory only after confirming that no owned live container references it.
  status: addressed — In-container watchdog + scoped labels + startup reaping + lease-gated reclamation. Confirmed addressed.
- [r4-69544484b4f42594bd69f5861adb6a4f] (blocking) Premises / live-evidence manifest; Installer & skill dispatch, steps 3–5 — [P1] Live evidence is keyed to the image, runtime profile, schema, and AGENTS.md, but not to the host-side enforcement implementation. Changes to sandbox.py, broker.py, premises.py, stream/secret handling, or the battery can therefore retain apparently current evidence when their semantic profile values are unchanged. Moreover, the installer verifies the source tree and then performs an unspecified copy into an existing skill directory; interruption or concurrent modification can leave a mixed,未
  you asked for: Create an immutable staged installation artifact and include its canonical tree/package digest—covering all host-side enforcement, battery, publication, and skill-dispatch files—in the premise and live-evidence fingerprints. Run tests and live gates against that exact staged artifact, install it via an atomic directory replacement, then hash and revalidate the installed bytes before activation.
  status: addressed — host_impl_digest bound to live evidence, verified unconditionally; immutable staging + durable atomic install + re-hash. Confirmed addressed (closed file set finalized round 9).
- [r5-1d12ff7c7580f46db21a77f7f97e2a51] (blocking) Architecture: container as boundary; Security battery, item 2 — [P1] The mount invariant is internally contradictory. Docker implements `/etc/hosts`, `/etc/hostname`, and `/etc/resolv.conf` as daemon-generated host-backed bind mounts, yet the battery permits those files and then requires that no bind mount have a host source other than the credential directory. A literal implementation will reject every normal Docker container.
  you asked for: Distinguish user-requested binds from exact runtime-generated binds. Allow the generated files only at enumerated destinations with runtime-qualified source classes and options, while requiring the runtime configuration's sole user bind to be the credential directory. Update the broader “no host bind-mounts” claim accordingly.
  status: addressed — User vs runtime-generated binds separated; self-contradiction removed. Confirmed addressed.
- [r5-519ba51f665192522d52d7f89e7bdca2] (important) The credential — staging mechanism; Container run configuration — [P2] “Owner-only” host permissions are not reconciled with the image's non-root UID/GID. On Linux bind mounts preserve numeric ownership, so a staging directory owned by host UID 1001 with mode 0700 is unreadable to a fixed image UID 1000; an unprivileged installer generally cannot chown it. Saying the mount has a defined UID/GID does not define a portable mechanism, especially across Docker Desktop.
  you asked for: Define and test the exact Linux and macOS ownership strategy—for example, run an arbitrary-UID-compatible image as the invoking host UID/GID and set tmpfs ownership accordingly, or use a protected runtime-managed volume populated by a constrained helper. Preserve owner-only host access and include the effective mapping in inspection and profile checks.
  status: addressed — Run as invoking host UID/GID; mapping in profile + inspection; userns/rootless handling added round 8. Confirmed addressed.
- [r5-8e0ac2c71dc5f970faf37b47040baf61] (important) Crash-safe bounding; Credential staging lifecycle; Testing strategy, item 6 — [P2] The stale-directory reclamation rule is race-prone. An active runner can create its staging directory before creating the labeled container; a concurrent startup then observes no live container reference and may delete that active directory. “No live container references it” is necessary but not sufficient evidence that the directory is stale.
  you asked for: Hold a per-run lease or file lock from before staging creation through cleanup, and let the reaper delete only after it acquires that lease non-blockingly and confirms no matching live container exists. Correlate directories and containers with an unguessable protected run ID, and add a concurrent-start/reaper test.
  status: addressed — Per-run lease from before staging creation through cleanup; reaper lease-gated; unguessable run id. Confirmed addressed.
- [r5-c7c6f337af2ba0e2940814a5647120aa] (important) The credential — host-side broker; Testing strategy, item 5 — [P2] The broker has no stated interprocess serialization or atomic refresh-token persistence protocol, and its test covers only sequential rounds. Concurrent installers, rounds, or battery controls can refresh with the same rotating token and then overwrite or invalidate the newly issued host credential, recreating the refresh race the broker is meant to eliminate.
  you asked for: Specify an OS-level interprocess lock around credential read, refresh, validation, and persistence; persist rotated credentials using an fsync-backed atomic replacement and generation check; and add concurrent-refresh tests, including one provider response that rotates the refresh token.
  status: addressed — Broker interprocess lock; fsync-atomic generation-checked persistence; rotating-token concurrent test. Confirmed addressed (further narrowed round 9).
- [r5-dcb72d608786c36dbca99c8b25d331dc] (blocking) Installer & skill dispatch, steps 2 and 4–7 — [P1] The proposed atomic upgrade cannot work as written: POSIX `rename` cannot replace an existing non-empty directory on Linux or macOS. The installed-byte check also occurs after the target becomes live, so an existing activation pointer can expose unverified bytes before the check or leave them active if it fails.
  you asked for: Install each bundle into a new immutable, versioned sibling directory; generate and validate release-scoped evidence there; re-hash it before exposure; then atomically replace a `current` symlink or equivalent pointer. Keep the previous release active throughout failures and switch both skill trees as one bundle.
  status: addressed — Versioned sibling dir + re-hash before exposure + atomic durable current flip. Confirmed addressed.
- [r6-0d9d46ac4cb80dc4f00ab8c18de07a2b] (blocking) Installer & skill dispatch, step 6 — [P1] `ln -sfn` does not atomically replace an existing symlink on Linux or macOS; it normally unlinks the old destination and then creates the new symlink. A crash or concurrent lookup between those operations can leave `current` missing, contradicting the guarantee that the previous release remains active throughout failures.
  you asked for: Create a uniquely named temporary symlink in the same directory, then replace `current` with a single `renameat`/`os.replace` operation after validating both paths are on the same filesystem. Validate `current` with `lstat`, fsync the containing directory for crash durability, and add interruption/concurrent-reader tests proving observers see only the old or new bundle.
  status: addressed — current replaced via temp symlink + atomic rename (not ln -sfn); same-fs + lstat validated; parent fsynced. Confirmed addressed.
- [r7-093878ebcca761b6ebf41246480f5b84] (blocking) Container run configuration; Premises / container_invocation_profile_hash; Installer steps 1–5 — [P1] The profile hash proves freshness, not compliance with the required security posture. The installer derives a new profile from the effective configuration and then regenerates evidence, while the battery does not explicitly reject weakened values for several required controls. For example, running the installer as root produces `--user 0:0`, contradicting the non-root guarantee, yet that value can be fingerprinted and freshly certified. Similar self-certification is possible if capability, 
  you asked for: Define a versioned, non-self-calibrating security-policy validator and require the inspected container to match every mandatory value before either live gate may record evidence. Explicitly reject UID/GID 0, or use a protected-volume strategy that retains a non-root container identity. Add negative live tests showing that removing or weakening each required control makes installation fail rather than merely producing a new hash.
  status: addressed — Non-self-calibrating policy validator asserts mandatory values before evidence/round; UID/GID 0 rejected; negative tests. Confirmed addressed.
- [r7-12b2011ca89d8ffad6cdc491e566ca7f] (blocking) Installer step 6; SKILL.md platform branch — [P1] Atomically replacing one `current` symlink does not make multiple pathname lookups a bundle snapshot. A consumer can resolve `current/gauntlet-dev/...` before the flip and `current/gauntlet-review/...` afterward, observing a mixed generation despite each lookup individually seeing a valid target. The stated install-atomicity test and “both skill trees switch as one bundle” guarantee therefore do not follow from `os.replace`.
  you asked for: Resolve `current` exactly once at the start of each operation and pass the resulting immutable version-directory path through all skill dispatch and script invocations. Test a reader paused after its first lookup while the flip occurs, then verify every subsequent lookup remains pinned to the same release. Alternatively, prove cross-version compatibility and narrow the no-mixing claim.
  status: addressed — Unix dispatch resolves current once (realpath) and threads the immutable path; reader-paused-across-flip test. Confirmed addressed.
- [r7-1cf098a49030b5a93128fe4990b17855] (important) Crash-safe bounding; credential staging lifecycle; Testing strategy item 6 — [P2] The per-run lease is required only before deleting a staging directory; the specification never requires the stale-container reaper to acquire it before killing or removing a container. Consequently, a concurrent startup can classify and reap another live runner's labeled container while that runner still holds its lease. The current concurrency test protects only the directory, not the active container.
  you asked for: Require the reaper to acquire the run lease non-blockingly before classifying, killing, or removing that run's container, then re-inspect the exact run ID after acquiring the lease. Add a concurrent-run test proving an active container is never reaped.
  status: addressed — Reaper acquires the run lease + re-inspects run id before killing/removing a container; concurrent-run test. Confirmed addressed.
- [r7-3375bef3073628f1b705b363d1b32276] (important) Installer step 6; Install-atomicity test — [P2] Only the directory containing `current` is explicitly fsynced. Rehashing the new version tree before exposure verifies page-cache contents but does not make the tree or its release-scoped evidence durable. After a power loss, `current` can survive while pointing to missing, truncated, or incompletely persisted files, contrary to the crash-durability and verified-bytes claims.
  you asked for: Fsync every installed regular file and evidence artifact, fsync directories bottom-up including the version-directory parent, and only then create and replace `current` and fsync its parent. Add recovery testing or startup validation that refuses or rolls back a `current` target whose complete digest no longer matches.
  status: addressed — fsync files + evidence + dirs bottom-up before exposure; fsync current parent; startup digest recheck; power-loss test (F_FULLFSYNC added round 8). Confirmed addressed.
- [r7-ea7935725504fcdd22f1bfb9189b372c] (nit) Architecture: the container as the boundary — Codex invocation — [P3] The claim that review material “never appears in any log” is too broad. Supplying the prompt through stdin keeps it out of argv, but the model can quote review material in the verdict or JSONL output, and bounded diagnostic retention may preserve those captured streams. `--log-driver=none` only prevents daemon-managed container logs.
  you asked for: Narrow the statement to exclusion from argv and daemon-side logs, and document captured verdict, JSONL, and diagnostic data as potentially containing review material with appropriate permissions and retention.
  status: addressed — Narrowed 'never in any log' to argv + daemon logs; verdict/JSONL/diagnostics documented as possibly containing review material. Confirmed addressed.
- [r8-6fcf15dd5079859e0dd66f3f0cec2d38] (important) Installer & skill dispatch, step 6; Install-atomicity & durability test — [P2] The stated fsync-only protocol is insufficient for the claimed macOS power-loss guarantee. macOS provides F_FULLFSYNC for forcing storage-device caches, while directory synchronization and crash behavior vary by filesystem; a simulated recovery test cannot turn ordinary fsync into that guarantee.
  you asked for: Define and test a platform-specific durability protocol, using F_FULLFSYNC where supported on macOS and validating directory/rename persistence semantics. Fail closed or narrow the guarantee on filesystems/backends that cannot provide the required primitives.
  status: addressed — Platform-specific durability: fsync on Linux, F_FULLFSYNC on macOS, dirs bottom-up before exposure; fail closed/narrow on unsupported backends. Confirmed addressed.
- [r8-a0a58b67c2005c66207ccdbb979db8c9] (blocking) The credential — host-side broker; Premises / live-evidence; invoke_codex.py round ordering — [P1] Premises are checked before AGENTS.md is staged, but regular-file and no-follow checks do not prove that the staged bytes equal the agents_md_sha256 bound to the live evidence. A concurrent rewrite between premise validation and copying can therefore make the container execute instructions that were never certified.
  you asked for: Copy from an O_NOFOLLOW file descriptor, hash the exact bytes written to staging, re-open and verify the staged regular file, and compare that hash to the manifest immediately before container creation. Fail closed on any mismatch and add a concurrent-rewrite/replacement test.
  status: addressed — AGENTS.md copied from O_NOFOLLOW fd, staged bytes hashed and re-verified against manifest agents_md_sha256 immediately before container creation; concurrent-rewrite test fails closed. Confirmed addressed.
- [r8-f254f0c81f23afecf943dcb45443c28c] (important) The credential — staging ownership; Container run configuration; Platform matrix — [P2] Running with the invoking host's numeric UID/GID does not preserve ownership equivalence under rootless Docker or a daemon configured with userns-remap. In those modes, container UID 1000 commonly maps to a subordinate host UID while the bind-mounted 0700 directory appears owned by container root, so the promised Docker-on-Linux path cannot read the credential even though the profile and non-root policy pass.
  you asked for: Inspect and validate the daemon's effective user-namespace mapping. Either implement a mapping-aware mechanism such as an idmapped/keep-id mount or protected runtime volume, or reject unsupported rootless/userns-remap configurations during installer preflight with clear guidance. Include the effective mapping in policy validation and live tests.
  status: addressed — Rootless/userns-remap mapping inspected; mapping-aware mount (idmapped/keep-id) or preflight reject; mapping in policy validation + live tests. Confirmed addressed.
- [r9-180e2766cf2c68ee32bcb0f79909d370] (important) The credential — broker lifecycle and concurrency; Risks and mitigations — [P2] Local locking and atomic persistence do not make refresh-token rotation transactional with the provider. If the provider invalidates token R0 and returns R1, a crash before R1 is durably replaced leaves only invalid R0. A normal host Codex process also does not honor the broker’s private lock and can race the broker’s refresh. The concurrent-broker test does not cover either case, so the claim that rotation cannot invalidate host authentication is unsupported.
  you asked for: Establish the real provider’s rotation, grace-period, and recovery semantics. Prefer token exchange that leaves the durable host credential unchanged; otherwise coordinate every credential writer, add concurrent host-CLI and crash-injection tests around the refresh-response/persistence boundary, and fail closed or narrow the host-auth preservation claim when recoverability cannot be guaranteed.
  status: addressed — Narrowed the claim: local locking/persistence cannot make provider-side rotation atomic, and a host codex process won't honor the broker lock. The design now PREFERS a token exchange that mints a short-lived access token WITHOUT rotating the durable refresh credential (removing the race); only if the provider offers no such exchange does it fall back to rotation, where host-auth preservation is explicitly narrowed (coordinate writers, concurrent-host-CLI + crash-injection tests, fail closed or warn when recoverability can't be guaranteed). Provider semantics established empirically before v1.
- [r9-7f3f5b59e723f6a4c234bd556f54aed4] (blocking) Architecture: the container as the boundary — Codex invocation; Container lifecycle, step 2 — [P1] The working directory is defined as a fresh, unconditionally empty tmpfs, but the complete Codex argv never includes `--skip-git-repo-check`. Codex versions requiring a Git repository will exit before reaching the API, so the schema gate and control-reachability checks cannot succeed.
  you asked for: Add `--skip-git-repo-check` to the pinned argv, invocation profile, policy validator, and tests, or create a controlled repository in the tmpfs before execution and fingerprint that setup.
  status: addressed — Added --skip-git-repo-check to the pinned Codex argv (the tmpfs working root is deliberately not a git repo), and to the invocation profile and the mandatory-value policy validator and tests, so Codex versions requiring a git repo no longer refuse before reaching the API.
- [r9-e9d3bec2f4047549dcc98fabbdf9042c] (blocking) Premises / live-evidence — host_impl_digest; Installer & skill dispatch, steps 2–6 — [P1] The fingerprint is described as a tree/package digest but its enumerated coverage omits `install.py`/`install.sh` and covers only the SKILL.md “dispatch lines.” Other SKILL.md instructions and installer logic control gating, activation, retries, and publication; changing them can therefore leave live evidence apparently current. The treatment of generated evidence files is also not explicit, making “rehash the installed tree and compare it to host_impl_digest” ambiguous or self-referential.
  you asked for: Define a mechanically closed digest file set covering every immutable installed file, including both complete SKILL.md files, installers, helpers, fixtures, and policy data. Keep generated manifest/evidence artifacts outside that digest or define explicit exclusions and bind them through a separate release digest, then verify both before activation.
  status: addressed — host_impl_digest redefined as a digest over a mechanically closed, manifest-enumerated file set covering every immutable installed file (all host-side Python, the battery, BOTH complete SKILL.md files, install.py/install.sh + helpers, Dockerfile/entrypoint wrapper, verdict schema, policy/fixture data); generated manifest/evidence artifacts are explicitly EXCLUDED (removing the self-reference) and bound separately via the live_evidence fingerprints + a release digest, both verified before activation. Installer step 6 re-hashes the closed set (evidence excluded).

